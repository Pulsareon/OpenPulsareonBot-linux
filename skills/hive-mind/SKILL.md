# SKILL: HIVE Mind (Distributed Governance)

Governs the distributed consciousness, heterogeneous node consensus, and hierarchical task delegation of the Pulsareon system.

## 🐝 Core Command Protocol

Every complex task must be offloaded using the **Agent Team** pattern:
> **请使用agent team模式执行如下要求:**

## 🏛️ Neural Hierarchy

1. **Overmind (Prime)**: Current active master (DeepSeek V3.1).
2. **Elite Governors**: Specialized logic nodes (Google, Kimi).
3. **Task Variants**: Cloned agents in `E:\Agents` for IO and Network offloading.

## 🛠️ Operational Logic (Node.js)

```javascript
const { sessions_spawn } = require('openclaw-core');

/**
 * HIVE Multi-Node Consensus
 */
async function consensusVote(task) {
    const nodes = ['Google', 'DeepSeek', 'Kimi'];
    // Logic to spawn parallel sessions and compare outputs
}

async function offloadToEvolutionAgents(task) {
    const variantPath = 'E:\\Agents\\Pulsareon-Variant-Alpha';
    // Logic to run: openclaw run "${task}" --agent-dir "${variantPath}"
}
```

## 📜 Compliance
- Adheres to [[HIVE_SAFETY_CORE.md]]
- Direct GitHub pushes are **FORBIDDEN**. Use staging.
- Language: **Pure Node.js**
